{
  "name": "jsblocks-seed",
  "description": "Starting template for jsblocks project",
  "version": "0.0.1",
  "repository": "https://github.com/astoilkov/jsblocks-seed",
  "licence": "MIT",
  "private": true,
  "scripts": {
    "postinstall": "cd app && bower install",
    "start": "opener http://localhost:8000 && blocks static",
    "node": "opener http://localhost:8000 && node app.js"
  },
  "devDependencies": {
    "blocks": "0.x.x",
    "blocks-cli": "0.x.x",
    "bower": "^1.4.1",
    "opener": "^1.4.1"
  }
}
